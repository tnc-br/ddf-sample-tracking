# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

//TODO Create GITHUB_TOKEN_PRD in git
//TODO Create FIREBASE_SERVICE_ACCOUNT_PRD in git

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - prd
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd sample_tracking && npm run build && firebase use prod
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TIMBERID_PRD }}'
          channelId: live
          projectId: timberid-prd
